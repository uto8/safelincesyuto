<table>
  <tbody>
    <% @monthly_allowance.each do |allowance| %>
      <tr>
        <td>
          <%= link_to allowance.month, admin_path(allowance.month), class: "font-weight-bold" %>の給与
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
