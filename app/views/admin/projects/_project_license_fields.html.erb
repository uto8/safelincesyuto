<div class="nested-fields mb-3 row">
  <div class="col-8">
    <%= f.collection_select :license_id, License.where.not(title: "隊長手当").where.not(title: "運転手当").where.not(title: "出張手当").where.not(title: "規制手当"), :id, :title, {prompt: "資格を選択してください"}, {class: 'form-control'} %>
  </div>
  <div class="col-4">
    <%= link_to_remove_association f do %>
      <div class="btn btn-outline-secondary">削除</div>
    <% end %>
  </div>
</div>
